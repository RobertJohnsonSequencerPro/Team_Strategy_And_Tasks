@using TeamStrategyAndTasks.Core.Enums

<MudChip T="string" Class="@($"status-chip--{Status}")" Size="Size.Small">
    @Status.ToString()
</MudChip>

@code {
    [Parameter, EditorRequired] public NodeStatus Status { get; set; }
}
